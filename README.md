# Studi — built demo

The compiled prototype, published so it can be tried in a browser. Source lives
in a private repository; this repo holds a build and nothing else, so its
history is not worth reading.

**Live:** https://studi-technologies.github.io/studi-demo/

Not indexed by search engines (`noindex` in `index.html`). Questions are drawn
from third-party CAT preparation material and are here to exercise the engine —
this is an engineering demo, not a publication of that material.
